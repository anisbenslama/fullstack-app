apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: taskflow
spec:
  selector:
    app: taskflow
    component: database
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP
  