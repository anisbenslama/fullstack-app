apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: taskflow
spec:
  selector:
    app: taskflow
    component: frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort
 